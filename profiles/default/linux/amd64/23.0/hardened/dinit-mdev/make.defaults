COMMON_FLAGS="-O3 -pipe -march=native -flto=16 -fgraphite-identity -floop-nest-optimize -fipa-pta -fno-semantic-interposition -fdevirtualize-at-ltrans"
CFLAGS="${CFLAGS} ${COMMON_FLAGS}"
CXXFLAGS="${CXXFLAGS} ${COMMON_FLAGS}"
FCFLAGS="${FCFLAGS} ${COMMON_FLAGS}"
FFLAGS="${FFLAGS} ${COMMON_FLAGS}"
LDFLAGS="${LDFLAGS} ${COMMON_FLAGS}"
FEATURES="parallel-fetch"
RUSTFLAGS="${RUSTFLAGS} -C target-cpu=native"
USE="${USE} mdev dinit"
